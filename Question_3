#include <iostream>
#include <ctime>

using namespace std;

int main() {
    time_t system_time = time(0);
    char* current_time = ctime(&system_time);
    cout << "Current Date and Time= "<<current_time<<endl;
    return 0;
}
